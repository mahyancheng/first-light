# First Light

This is a clean-room game. Do not import code, assets, data, save formats or screen components from Frontier Capital/lm. Retain only the general AI-company strategy premise.

The server owns state. Every player mutation requires a stable request identity and optimistic revision; economic mutations and receipts commit together. The model proposes, the player reviews, and the engine settles. Do not manufacture delivery, progress or receipts from prose.

Test gameplay changes deterministically, including cash conservation and failure paths. Test visible flows in a browser. Keep hypothetical or unimplemented features labelled honestly. Scenario coefficients are game balance, not historical facts.

All release source and configuration belongs in GitHub. Build images in GitHub Actions and deploy immutable commit tags. Secrets and saves remain in server-side persistent storage, never in GitHub or images. Do not overwrite the old game's state or deployment.

User notifications and model wakeups are event-based only. No scheduled model turns or Desktop bridge heartbeats. Existing SAG recall/writeback workflow applies during actual task work. Use project first-light; reference lm #1049 only for provenance of the previous project and deployment.
